import { RoleCheckDirective } from './role-check.directive';

describe('RoleCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new RoleCheckDirective();
    expect(directive).toBeTruthy();
  });
});
